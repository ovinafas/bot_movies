services:
  - type: web
services → لیست سرویس‌هایی که Render اجرا می‌کنه.

type: web → یعنی این سرویس وب/HTTP هست و نیاز داره روی پورت قابل دسترسی اینترنت باشه.

برای ربات تلگرام که از Webhook استفاده می‌کنه، وب سرویس لازمه چون تلگرام به URL ما پیام میفرسته.

yaml
Copy code
    name: uptv-movie-bot
نام سرویس روی Render.

هر چیزی می‌تونه باشه ولی بهتره مرتبط با پروژه باشه (uptv-movie-bot).

این اسم برای مدیریت سرویس در داشبورد Render استفاده میشه.

yaml
Copy code
    env: python
محیط اجرا یا Runtime سرویس رو مشخص می‌کنه.

یعنی این سرویس Python هست و Render خودش محیط مجازی با Python آماده می‌کنه.

yaml
Copy code
    region: frankfurt
Region یا محل سرور

سرور شما در فرانکفورت آلمان قرار می‌گیره.

میشه نزدیک کاربرهای اصلی یا سریع‌ترین دسترسی اینترنت انتخاب بشه.

yaml
Copy code
    buildCommand: pip install -r requirements.txt
دستور ساخت یا Build service

این دستور همه پکیج‌های لازم پروژه رو نصب می‌کنه.

requirements.txt شامل تمام کتابخانه‌های Python مثل python-telegram-bot و requests و beautifulsoup4 هست.

yaml
Copy code
    startCommand: python bot.py
دستور اصلی برای راه‌اندازی سرویس

وقتی Render سرویس رو اجرا می‌کنه، این دستور اجرا میشه و ربات شروع به کار می‌کنه.

در پروژه تو، این bot.py هست که Webhook رو راه‌اندازی کرده و آماده دریافت پیام‌هاست.

💡 جمع‌بندی:
این تنظیمات میگه:

یک وب سرویس Python بساز (type: web)

اسمش uptv-movie-bot باشه

در منطقه فرانکفورت اجرا بشه

اول پکیج‌ها نصب بشن (buildCommand)

بعد bot.py اجرا بشه (startCommand)