python-telegram-bot[webhooks]==20.3
این پکیج اصلی ربات تلگرامه.

[webhooks] میگه نسخه Webhook هم نصب بشه، چون Telegram وقتی از Webhook استفاده می‌کنه نیاز به این امکانات داره.

==20.3 یعنی دقیقا نسخه 20.3 نصب بشه تا با کد تو سازگار باشه.

nginx
Copy code
requests
برای ارسال درخواست HTTP به سایت‌ها یا API‌ها.

توی ربات میخوای فیلم‌ها از uptv.ir بگیری، پس requests لازمه.

nginx
Copy code
beautifulsoup4
برای وب‌اسکرپینگ HTML.

به کمک این کتابخانه می‌تونی محتوای صفحات HTML سایت uptv.ir رو بخونی و اطلاعات فیلم‌ها (اسم، عکس، لینک) استخراج کنی.

💡 نکته:

ترتیب مهم نیست ولی بهتره همیشه python-telegram-bot اول باشه.

حتما [webhooks] باشه، وگرنه روی Render با خطای Webhook مواجه میشی